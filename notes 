export DB_HOST=mongodb://10.0.3.4:27017/posts

# Log into virtural machine 

sudo apt update -y
sudo DEBIAN_FRONTEND=noninteractive apt upgrade -y
sudo DEBIAN_FRONTEND=noninteractive apt-get install nginx -y
curl -fsSL https://deb.nodesource.com/setup_20.x -o setup_nodejs.sh
sudo DEBIAN_FRONTEND=noninteractive bash setup_nodejs.sh
node -v
sudo DEBIAN_FRONTEND=noninteractive apt-get install nodejs -y
node -v
exit
ls
   11  sudo apt-get update
   12  DEBIAN_FRONTEND=noninteractive apt-get install unzip
   13  sudo DEBIAN_FRONTEND=noninteractive apt-get install unzip
   14  unzip nodejs20-sparta-test-app-2.zip 
   15  cd nodejs20-sparta-test-app-2/
   16  ls
   17  cd app
   18  npm install
   19  node app.js
   20  npm start 
   21 
   
export DB_HOST=mongodb://10.0.3.4:27017/posts
printenv DB_HOST
npm install
npm start
node seeds/seed.js
npm start


In the VM for the web, I need Nginx for the web server and Node torun the website. 
they both must be installed 
I also need unzip
